# build outputs
dist/
Release/
Release2/
bin/
obj/
*.pdb
# local test artifacts
test-pkgs/
# payload ELF builds stay local-only, never push
payload/*.elf
# installer output (built locally per release)
PkgSender-Setup-*.exe
PkgSender-macOS-*.zip
.DS_Store
# IDE
.vs/
*.user
